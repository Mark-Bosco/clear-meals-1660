name: Publish preview update with APK release

on:
  workflow_dispatch:
  push:
    branches: ['*']

jobs:
  create_preview_build:
    runs-on: ubuntu-latest
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: ⚙️ Set up Node.js (for EAS CLI)
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 🛠 Install dependencies
        run: |
          sudo apt update && sudo apt install gh jq wget -y
          npm install -g eas-cli
          npm install dotenv  # Install dotenv for loading environment variables

      - name: 🧪 Debug - Check if EXPO_TOKEN is present
        run: echo "EXPO_TOKEN is set? => ${EXPO_TOKEN:+yes}"

      - name: 🚀 Trigger EAS Preview Build
        run: |
          echo "Triggering preview build..."
          eas build --platform android --profile preview --non-interactive --no-wait

      - name: ⏳ Wait for build to complete
        run: |
          echo "Waiting for build $BUILD_ID to complete..."
  
          MAX_ATTEMPTS=120  # 2 hours
          ATTEMPTS=0
          STATUS=""
  
          while [[ "$STATUS" != "finished" && "$STATUS" != "errored" && $ATTEMPTS -lt $MAX_ATTEMPTS ]]; do
            echo "🔍 Checking build status (attempt $ATTEMPTS)..."
            STATUS=$(eas build:inspect --json --id $BUILD_ID | jq -r '.status')
            echo "Current status: $STATUS"
            if [[ "$STATUS" != "finished" && "$STATUS" != "errored" ]]; then
              ATTEMPTS=$((ATTEMPTS + 1))
              sleep 60
            fi
          done
  
          if [[ "$STATUS" == "finished" ]]; then
            echo "✅ Build completed successfully!"
          elif [[ "$STATUS" == "errored" ]]; then
            echo "❌ Build failed."
            exit 1
          else
            echo "❌ Build timed out after $MAX_ATTEMPTS attempts."
            exit 1
          fi
      


      - name: 📦 Download APK
        run: |
          APK_URL=$(eas build:list --status finished --platform android --json | jq -r '.[] | select(.metadata.buildProfile == "preview") | .artifacts.buildUrl' | head -n 1)

          if [ -z "$APK_URL" ]; then
            echo "❌ No APK found. Aborting."
            exit 1
          fi

          echo "⬇️ Downloading APK from: $APK_URL"
          wget -O preview.apk "$APK_URL"

          if [ ! -f preview.apk ]; then
            echo "❌ APK download failed. Aborting."
            exit 1
          fi

      - name: 🚀 Publish GitHub Release with APK
        run: |
          RELEASE_TAG="preview-$(date +'%Y%m%d%H%M')"
          RELEASE_NAME="Preview Build - $(date +'%Y-%m-%d %H:%M:%S')"

          echo "📢 Creating GitHub release: $RELEASE_TAG"
          gh release create "$RELEASE_TAG" preview.apk \
            --title "$RELEASE_NAME" \
            --notes "Automated preview build release for internal testing."
