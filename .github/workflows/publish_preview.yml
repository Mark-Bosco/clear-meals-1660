name: Publish preview update with APK release

on:
  push:
    branches: ['*']

jobs:
  create_preview_build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: ⚙️ Set up Node.js (for eas-cli)
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 🛠 Install system dependencies
        run: |
          sudo apt update
          sudo apt install gh jq wget -y
          npm install -g eas-cli

      - name: 🔐 Authenticate and trigger EAS build (Preview)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          eas login --token "${EXPO_TOKEN}"

          echo "🚀 Triggering preview build..."
          eas build --platform android --profile preview --non-interactive --no-wait

      - name: ⏳ Wait for latest preview build to finish
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          echo "🕒 Polling for finished preview build..."

          BUILD_ID=""
          while [ -z "$BUILD_ID" ]; do
            BUILD_ID=$(eas build:list --status finished --platform android --json | jq -r '.[] | select(.metadata.buildProfile == "preview") | .id' | head -n 1)
            if [ -z "$BUILD_ID" ]; then
              echo "⏳ Waiting for preview build to finish..."
              sleep 30
            fi
          done

          echo "✅ Found finished build ID: $BUILD_ID"
          echo "BUILD_ID=$BUILD_ID" >> $GITHUB_ENV

      - name: 📦 Get APK download URL and download
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          APK_URL=$(eas build:list --status finished --platform android --json | jq -r '.[] | select(.metadata.buildProfile == "preview") | .artifacts.buildUrl' | head -n 1)

          if [ -z "$APK_URL" ]; then
            echo "❌ APK URL not found. Aborting."
            exit 1
          fi

          echo "⬇️ Downloading APK..."
          wget -O preview.apk "$APK_URL"

          if [ ! -f preview.apk ]; then
            echo "❌ APK download failed. Aborting."
            exit 1
          fi

      - name: 🚀 Publish GitHub Release with APK
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          RELEASE_TAG="preview-$(date +'%Y%m%d%H%M')"
          RELEASE_NAME="Preview Build - $(date +'%Y-%m-%d %H:%M:%S')"

          echo "📢 Creating GitHub release: $RELEASE_TAG"
          gh release create "$RELEASE_TAG" preview.apk \
            --title "$RELEASE_NAME" \
            --notes "Automated preview build release for internal testing."
